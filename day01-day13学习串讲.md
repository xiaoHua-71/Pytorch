# PyTorch 入门串讲：从一个数字到图片和序列

这份笔记把 `day01` 到 `day13` 串成一条完整的学习路线。你可以把神经网络想成一个“会调参数的函数”：输入数据，输出预测；如果预测错了，就根据错误调整参数。后面的线性回归、分类、CNN、RNN，变化的主要是输入数据和模型结构，训练骨架始终相似。

## 先看全路线

```text
数据 -> 模型 -> 预测 -> 损失 -> 反向传播求梯度 -> 优化器更新参数
  ^                                                        |
  +---------------------- 重复很多轮 -----------------------+
```

各天的关系可以这样理解：

| 阶段 | 内容 | 解决的问题 |
| --- | --- | --- |
| day01 | 机器学习和神经网络概念 | 模型到底在学习什么 |
| day02 | 线性模型、损失、穷举 | 怎样评价预测好不好 |
| day03 | 梯度下降 | 怎样高效寻找参数 |
| day04 | 反向传播 | 多层模型怎样计算梯度 |
| day05 | PyTorch 线性回归 | 让框架自动求导和更新参数 |
| day06 | 逻辑斯蒂回归 | 从预测数值变成二分类 |
| day07 | 多维特征 | 一个样本有多个输入特征 |
| day08 | Dataset、DataLoader | 大数据如何分批读取 |
| day09 | 多分类 | 从多个互斥类别中选一个 |
| day10-11 | CNN | 利用图片的空间结构 |
| day12-13 | RNN、LSTM、GRU | 利用序列前后的关系 |

## 1. 机器学习到底在做什么？（day01）

假设我们有很多样本，每个样本都包含输入 `x` 和答案 `y`：

```text
(x, y) = (房屋特征, 房价)
(x, y) = (图片, 图片类别)
(x, y) = (前20天温度, 第21天温度)
```

模型是一个带参数的函数，例如：

```text
预测值 y_hat = f(x; 参数)
```

训练不是把答案直接写进程序，而是让模型自己调整参数，使 `y_hat` 越来越接近 `y`。训练完成后，把没见过的新输入交给模型，这一步叫推理（inference）。

机器学习中常见两类任务：

- **回归**：输出连续数值，例如房价、温度、销量。
- **分类**：输出类别，例如猫/狗、数字 0~9、正面/负面。

“特征”就是输入中可供模型利用的数字。房屋面积、房间数是特征；一张图片的像素也是特征；一句话中的词编号也是特征的另一种表示。

## 2. 从最简单的线性模型开始（day02）

先只用一个数字预测另一个数字：

```text
y_hat = w * x + b
```

`w` 是权重，表示输入的重要程度；`b` 是偏置，表示整体平移。它们就是模型要学习的参数。

### 损失函数：给预测打分

模型预测后，需要一个数字表示“错得有多严重”。均方误差 MSE 是回归常用的损失：

```text
MSE = mean((y_hat - y) ** 2)
```

损失越小越好。训练的目标不是让某一个样本刚好正确，而是让一批样本的平均损失尽量小。

day02 用穷举法尝试很多个 `w`：每次计算 MSE，最后选损失最小的参数。这适合只有一个参数的玩具例子，但参数一多就会变得极慢。

## 3. 梯度下降：沿着下坡方向走（day03）

把损失想成一座山的高度。梯度指向当前位置最陡的上坡方向，因此参数应该沿梯度的反方向更新：

```text
参数 = 参数 - 学习率 * 梯度
```

对线性模型来说：

```text
w = w - lr * grad_w
b = b - lr * grad_b
```

`lr` 是学习率，决定步子大小。太大可能越过谷底甚至发散，太小则训练很慢。

根据每次使用的数据量，梯度下降可以分为：

- 批量梯度下降：每次使用全部数据。
- 随机梯度下降：每次使用一个样本。
- 小批量梯度下降：每次使用一小批样本，深度学习中最常见。

## 4. 反向传播：自动把梯度传回去（day04）

前向传播是从输入走到损失：

```text
x -> 线性计算 -> y_hat -> loss
```

反向传播则从损失倒着走，使用链式法则计算每个参数对损失的影响：

```text
∂loss/∂w = (∂loss/∂y_hat) * (∂y_hat/∂w)
```

一句话记忆：**前向传播算结果，反向传播算梯度，梯度下降更新参数。**

多层网络的好处是表达能力更强，但手工推导每一层的梯度很麻烦。反向传播通过复用上游梯度，让每层只计算一次。

## 5. PyTorch 的固定训练模板（day05）

PyTorch 用自动求导代替手工计算梯度。一个最小训练循环通常是：

```python
for inputs, targets in train_loader:
    predictions = model(inputs)       # 1. 前向传播
    loss = criterion(predictions, targets)  # 2. 计算损失

    optimizer.zero_grad()              # 3. 清空旧梯度
    loss.backward()                    # 4. 自动反向传播
    optimizer.step()                   # 5. 更新参数
```

对应关系如下：

| PyTorch 写法 | 它在做什么 |
| --- | --- |
| `nn.Module` | 组织模型结构和可学习参数 |
| `forward` | 定义输入怎样变成输出 |
| `criterion` | 定义预测错多少 |
| `loss.backward()` | 自动计算参数梯度 |
| `optimizer.step()` | 按梯度更新参数 |
| `zero_grad()` | 清掉上一批留下的梯度 |

`model(x)` 是标准调用方式，它会自动进入 `forward`。不要直接把 `model.forward(x)` 当作日常调用方式。

## 6. 张量形状：先看懂每个数字代表什么

形状不是装饰信息，它决定了层能不能接上。常见符号如下：

| 符号 | 含义 |
| --- | --- |
| `N` 或 `batch` | 一次并行处理的样本数量 |
| `D` 或 `feature` | 每个样本的特征数量 |
| `C` | 通道数，或分类类别数，需结合上下文判断 |
| `T` 或 `sequence_length` | 序列中的时间步/词数 |
| `H, W` | 图片高度和宽度 |

例如：

```text
(N, D)       = N 个样本，每个样本 D 个特征
(N, C)       = N 个样本，每个样本输出 C 个类别分数
(N, C, H, W) = N 张图片、C 个通道、高 H、宽 W
(N, T, F)    = N 条序列、每条 T 步、每步 F 个特征
```

### `batch` 和 `sequence_length` 的区别

```text
(batch, sequence_length) = (3, 5)
```

表示一次处理 3 条序列，每条序列有 5 个位置。例如 3 句话，每句话最多 5 个词。它不是“3 个词、5 条句子”，顺序要看约定。

当词编号经过嵌入层后：

```text
(N, T) -> Embedding -> (N, T, embedding_dim)
```

`Embedding`（嵌入层）可以理解为一个可训练的查表：词编号 `7` 被查成一个长度为 128 的向量。这个向量叫嵌入向量，不是“键入向量”。编号本身没有语义，向量是模型训练出来的表示。

## 7. 二分类：线性模型加概率解释（day06）

二分类只有两个类别，例如通过/不通过。先得到任意实数 `logit`：

```text
logit = w*x + b
```

再用 Sigmoid 把它变成 0 到 1 之间的概率：

```text
probability = sigmoid(logit)
```

PyTorch 推荐使用 `BCEWithLogitsLoss`，它已经把 Sigmoid 和二元交叉熵稳定地合在一起。因此训练时模型直接输出 logits，不要在 `forward` 里手动加 Sigmoid；预测时才使用 `torch.sigmoid()`。

## 8. 多维特征：一个样本不只一个数字（day07）

现实中的输入经常是一个向量：房屋的面积、房间数、房龄可以组成 3 个特征：

```text
y_hat = w1*x1 + w2*x2 + w3*x3 + b
```

输入形状是 `(N, D)`。例如 `(5, 3)` 表示 5 个样本，每个样本 3 个特征。对应的层是：

```python
layer = nn.Linear(in_features=3, out_features=1)
```

规则只有一条：**`Linear` 的 `in_features` 必须等于输入最后一维。** 单个样本也保留 batch 维度，写成 `(1, 3)`，不要写成 `(3,)`。

## 9. Dataset 和 DataLoader：把数据送进训练循环（day08）

数据少时可以直接 `model(x_data)`；数据大时需要分批处理。

- `Dataset`：定义“数据集中有哪些样本，以及如何取第几个样本”。
- `DataLoader`：把样本组成 batch，并可打乱顺序、并行读取。

```python
for inputs, targets in train_loader:
    # inputs.shape 可能是 (32, 3)
    predictions = model(inputs)
```

一个 epoch 是完整遍历一次数据集，不是一次 batch。训练集通常 `shuffle=True`；验证集和测试集通常不打乱。验证时要使用：

```python
model.eval()
with torch.no_grad():
    ...
```

训练集用于更新参数，验证集用于选超参数和最佳模型，测试集只在最后评价泛化能力。

## 10. 多分类：输出每个类别的分数（day09）

如果一个样本只能属于一个类别，例如数字 0~9，那么最后一层输出类别数个 logits：

```python
nn.Linear(feature_dim, class_count)
```

输出形状是 `(N, C)`，标签形状是 `(N,)`，标签类型是 `torch.long`，类别编号从 `0` 到 `C-1`。

训练时直接把 logits 交给 `CrossEntropyLoss`，不要手动加 Softmax：

```python
loss = nn.CrossEntropyLoss()(logits, targets)
predictions = logits.argmax(dim=1)
```

需要展示概率时才使用 `softmax(dim=1)`。二分类用 Sigmoid，多分类用 Softmax；这两个概念不要混用。

## 11. CNN：为什么图片不能只当成长向量（day10）

图片的相邻像素通常有空间关系。把 `28 x 28` 图片直接拉平成 784 个数字，会丢失“谁和谁相邻”的结构。CNN 保留空间布局，用小卷积核在局部区域滑动，逐层学习：

```text
边缘 -> 纹理/角点 -> 局部形状 -> 物体整体
```

PyTorch 的图片输入是：

```text
(N, C, H, W)
```

灰度图 `C=1`，RGB 彩图 `C=3`。一个典型结构：

```text
图片 -> Conv2d -> ReLU -> MaxPool
     -> Conv2d -> ReLU -> MaxPool
     -> Flatten -> Linear -> logits
```

- `Conv2d` 提取局部特征；
- `ReLU` 提供非线性；
- `MaxPool2d` 缩小高宽、保留明显响应；
- `Flatten` 只改变形状，不学习参数；
- `Linear` 根据特征完成分类。

## 12. CNN 变深后怎么办（day11）

真实项目中，CNN 常配合这些工具：

- **BatchNorm**：让训练更稳定；
- **Dropout**：训练时随机丢弃部分输出，减少过拟合；
- **数据增强**：对训练图片做合理的翻转、裁剪、旋转；
- **残差连接**：让梯度可以沿 shortcut 更容易传到前面；
- **全局平均池化**：把 `(N, C, H, W)` 变成 `(N, C, 1, 1)`，减少手算 Flatten 尺寸和参数；
- **学习率调度、早停**：让训练后期更稳，并避免验证集效果变差；
- **迁移学习**：使用已经学过通用视觉特征的预训练模型。

BatchNorm 和 Dropout 都依赖训练/评估模式，所以验证和预测前必须 `model.eval()`。

## 13. RNN：数据有顺序时，不能只看当前输入（day12）

图片主要关注空间邻近关系；文本、温度、语音、销量还需要关注先后顺序。RNN 每次读取一个时间步，并把上一时刻的隐藏状态带过来：

```text
x_t + h_(t-1) -> h_t
```

使用 `batch_first=True` 时，RNN 输入形状是：

```text
(N, T, F)
```

例如 `(32, 20, 4)` 表示 32 条序列，每条有 20 个时间步，每步有 4 个特征。对文本来说，词编号先经过 Embedding，得到的 `embedding_dim` 就是 RNN 的 `input_size`。

普通 RNN 会返回：

```python
output, hidden = rnn(x)
```

`output` 保存每个时间步的隐藏状态，形状通常是 `(N, T, H)`；`hidden` 保存最后时刻的状态，形状通常是 `(num_layers, N, H)`。整段序列分类时常取 `output[:, -1, :]` 接分类层。

## 14. LSTM、GRU 和时间序列（day13）

普通 RNN 在长序列中容易梯度消失或早期信息被覆盖。LSTM 增加 `cell` 长期记忆通道，并用遗忘门、输入门、输出门控制信息；GRU 用更少的门实现类似的记忆控制。

选择可以先这样记：短序列学习原理用 RNN；想保留长期信息试 LSTM；想要更轻量、速度更快试 GRU。

时间序列通常用滑动窗口制作样本：

```text
[第1~20天] -> 第21天
[第2~21天] -> 第22天
```

预测未来时必须按时间顺序划分训练、验证、测试集，不能随机把未来数据混入训练。连续值预测使用 `MSELoss` 或 `L1Loss`，分类才使用 `CrossEntropyLoss`。

## 15. 把所有任务放回同一个框架

虽然模型不同，但训练过程可以统一成下面这张图：

```text
输入数据
  -> 模型 forward
  -> logits 或连续预测值
  -> 损失函数 criterion
  -> loss.backward()
  -> optimizer.step()
```

区别主要在“输入形状”和“最后的输出解释”：

| 任务 | 输入例子 | 输出 | 常用损失 |
| --- | --- | --- | --- |
| 一元/多元回归 | `(N, D)` | `(N, 1)` 连续值 | `MSELoss` / `L1Loss` |
| 二分类 | `(N, D)` | `(N, 1)` logit | `BCEWithLogitsLoss` |
| 多分类 | `(N, D)` 或图片 | `(N, C)` logits | `CrossEntropyLoss` |
| 图片分类 | `(N, C, H, W)` | `(N, class_count)` | `CrossEntropyLoss` |
| 序列分类 | `(N, T, F)` | `(N, class_count)` | `CrossEntropyLoss` |
| 序列回归 | `(N, T, F)` | `(N, 1)` 或 `(N, output_dim)` | `MSELoss` / `L1Loss` |

## 16. 最容易混淆的词

### 参数、超参数、特征

- **参数**：模型通过训练学出来的，如权重、偏置。
- **超参数**：训练前人为设置的，如学习率、batch size、层数、hidden size。
- **特征**：输入数据中的一列信息，如房屋面积、像素值、每天的温度。

### logit、概率、类别

- **logit**：模型最后的原始分数，可以是任意实数。
- **概率**：经过 Sigmoid 或 Softmax 后的 0~1 数值。
- **类别**：根据阈值或 `argmax` 得到的最终编号。

### epoch、batch、iteration

- **batch**：一次取出的样本小批次。
- **iteration**：处理一个 batch 并更新一次参数。
- **epoch**：完整遍历一次训练集。

### `train()`、`eval()`、`no_grad()`

- `model.train()`：训练模式，Dropout 开启，BatchNorm 使用当前 batch 统计量。
- `model.eval()`：评估模式，Dropout 关闭，BatchNorm 使用累计统计量。
- `torch.no_grad()`：不记录梯度，常用于验证和预测。

## 17. 一份可复用的检查清单

遇到一个新任务时，可以按这个顺序思考：

1. 我的任务是回归、二分类、多分类，还是序列任务？
2. 一个样本的输入形状是什么？batch 维度在哪里？
3. 模型最后输出的形状是什么？标签形状和类型是否匹配？
4. 损失函数是否和任务对应？是否错误地提前加了 Softmax 或 Sigmoid？
5. 训练集、验证集、测试集是否分开？时间序列是否泄漏未来？
6. 训练循环是否包含 `zero_grad -> backward -> step`？
7. 验证和预测是否使用 `eval()` 与 `no_grad()`？
8. 训练损失下降但验证效果变差时，是否需要正则化、数据增强、早停或迁移学习？

## 总结

这 13 天实际上只是在逐步回答三个问题：

1. **模型如何学习？** 用损失衡量错误，用反向传播求梯度，用优化器更新参数。
2. **不同数据如何表示？** 表格数据常用 `(N, D)`，图片用 `(N, C, H, W)`，序列用 `(N, T, F)`，文字还要先经过 Embedding。
3. **什么结构适合什么数据？** 线性层适合简单向量，CNN 适合空间结构，RNN/LSTM/GRU 适合有顺序的序列。

只要先确认任务类型、数据形状、输出形状和损失函数，后面的代码通常都可以套用同一个 PyTorch 训练骨架。
